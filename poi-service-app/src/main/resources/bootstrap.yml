server:
  port: 8808

spring:
  application:
    name: poi-service
  cloud:
    config:
      discovery:
        enabled: true
management.security.enabled: false

eureka:
 client:
  serviceUrl:
   defaultZone: http://localhost:8761/eureka/
 healthcheck:
   enabled: true